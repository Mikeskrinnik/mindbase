[workspace]
members = ["packages/shared", "packages/api", "packages/worker", "packages/mcp-server", "packages/collector", "packages/sync-agent"]

[tool.uv]
dev-dependencies = ["pytest>=8.3.0", "pytest-asyncio>=0.24.0", "ruff>=0.8.0"]
